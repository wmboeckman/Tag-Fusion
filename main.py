import app.interface.cli as cli

# TODO:
# - Build rudimentary tag generator (no extensive customization, simplistic design.)
# - Develop boilerplate cli app to interface with the tag generator pipeline.
# - Expand on the generator application to provide customization options (and maybe a node-based pipeline?)
# - Build rudimentary UI interface

if __name__ == '__main__':
    cli.cli()
